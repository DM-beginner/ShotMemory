[project]
name = "shotmemory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.1.1",
    "pyexiftool>=0.5.6",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
